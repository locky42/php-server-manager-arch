# php-server-manager
###### do this `chmod +x ./install.sh` and run this `sudo ./install.sh`

### server command:
- #### `server start`
- #### `server restart`
- #### `server stop`

### addhost comand:
- #### `addhost site.loc`
